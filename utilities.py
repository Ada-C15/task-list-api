


# def sort_tasks(tasks):
    
    # q = request.args.get("sort")
    # tasks_response = []

    # for task in tasks:
    
    #     tasks_response.append(task.create_json())

    #     if q == "asc":
        
    #         sorted_tasks = task.sort()
    #         tasks_response.append(sorted_tasks)
                    
    #     elif q == desc:
    #         sorted_tasks = task.sort(reverse=True)
    #         tasks_response.append(sorted_tasks) 

    #     return make_response(jsonify(tasks_response), 200)

    # def make_sorted_json():

    #     if completed_at == None:
    #     completed = False

    #     else:
    #         completed = True

    #     return {
    #             'id' : self.id,
    #             'title' : self.title,
    #             'description' : self.description,
    #             'is_complete' : completed
    #     }
